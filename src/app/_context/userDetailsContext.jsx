// We added an underscore before "context" so Next.js does not treat the "context" folder as a route in the App Router.

import { createContext } from "react";


export const UserDetailsContext = createContext();
